Examples
========

Each example is an independently synthesizable CDK application. Placeholder
account IDs, certificate ARNs, hosted-zone IDs, and domain names must be
replaced before deployment.

Generated CloudFront Hostname
-----------------------------

Omit custom-domain configuration to use the hostname generated by CloudFront.
The output exposes that hostname after deployment.

.. literalinclude:: ../../examples/cloudfront-domain/app.py
   :language: python
   :linenos:

External DNS and Imported Certificate
-------------------------------------

Import an ACM certificate from ``us-east-1`` and configure the CloudFront
alternate domain name. Manage the DNS record with the external provider by
pointing it at the output distribution hostname.

.. literalinclude:: ../../examples/external-dns/app.py
   :language: python
   :linenos:

Route 53 Aliases and Certificate
--------------------------------

Supply an existing public hosted zone to create a DNS-validated ACM
certificate and Route 53 A and AAAA aliases. Certificate creation requires an
explicit ``us-east-1`` stack environment.

.. literalinclude:: ../../examples/route53-managed/app.py
   :language: python
   :linenos:

Access Logging
--------------

Enable CloudFront access logging and choose how long log objects remain in the
construct-created bucket. Logging adds storage and request costs.

.. literalinclude:: ../../examples/access-logging/app.py
   :language: python
   :linenos:

Externally Managed Content
--------------------------

Omit ``site_content_path`` when another deployment system owns object upload
and cache metadata. The construct then creates no ``BucketDeployment``. Use
the outputs in an authorized deployment workflow to synchronize content and
invalidate changed CloudFront paths.

.. literalinclude:: ../../examples/external-content/app.py
   :language: python
   :linenos:

Custom Content Security Policy
------------------------------

Replace the restrictive default only when the site needs explicitly approved
external origins. Keep directives narrow and test the emitted response header
in a representative environment.

.. literalinclude:: ../../examples/custom-csp/app.py
   :language: python
   :linenos:
